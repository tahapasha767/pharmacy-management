alter table and drop the login and logout times from the employees table
ALTER TABLE employees
DROP COLUMN login column name,
DROP COLUMN logout column name;
pay attention i forgot above thing but copy paste the below 
ALTER TABLE products
DROP COLUMN storeID;
ALTER TABLE stores
DROP COLUMN managerid;
DROP TABLE IF EXISTS StockDistribution;  
CREATE TABLE logins (
    id SERIAL PRIMARY KEY,
    employee_id INT,
    log_in TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    log_out TIMESTAMP DEFAULT NULL,
    hours_worked INTERVAL,
    FOREIGN KEY (employee_id) REFERENCES employees(employeeid)
);
ALTER TABLE SalesTransactions
DROP COLUMN transactionid;
ADD COLUMN transactionid SERIAL PRIMARY KEY;